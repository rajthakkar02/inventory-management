<% content_for(:title) { "Add Staff — MobileStock Pro" } %>

<div class="page-header">
  <div>
    <h1 class="page-title">➕ Add Staff Member</h1>
    <p class="page-subtitle">Create a new staff account</p>
  </div>
  <%= link_to users_path, class: "btn btn-secondary" do %>← Back to Staff<% end %>
</div>

<%= render "form", user: @user %>
